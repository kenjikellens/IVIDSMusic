/**
 * IVIDS Music - Configuration
 * 
 * In Native Android Mode, the app intercepts all requests to localhost:3000
 * and handles them natively in Kotlin. No physical server needed.
 */
export const Config = {
    SERVER_URL: "http://localhost:3000"
};
