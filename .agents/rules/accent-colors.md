---
trigger: always_on
---

# Accent Color Rules

- Always use the defined CSS accent colors at all times!
- When the user mentions "green" or "colored" elements, they might mean the theme's active accent color. In this case, always ask if they mean the accent color rather than hardcoding a random color.
